dev:
	docker run -it -v ${PWD}:/projects -p 8080:8080 demotest npm run serve
build:
	docker run -it -v ${PWD}:/projects -p 8080:8080 demotest npm run build
lint:
	docker run -it -v ${PWD}:/projects -p 8080:8080 demotest npm run lint